[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  # Optionnel: spécifier une version de Bun (Netlify détecte automatiquement bun.lockb)
  # BUN_VERSION = "1.3.3"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
